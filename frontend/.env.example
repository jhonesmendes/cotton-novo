# Host:porta do backend usado pelo proxy /api do Vite dev server.
# Em Docker Compose isso é setado automaticamente para "http://backend:3001".
# Rodando fora do Docker, o padrão já é http://localhost:3001 (não precisa setar).
VITE_PROXY_TARGET=http://localhost:3001
