# ─── Variáveis de produção (Vite usa este arquivo no build) ───────────────────
# O Nginx faz proxy de /api → localhost:3001, então a URL base é o próprio domínio
VITE_API_URL=http://cotton.jhontisystem.com.br/api
